{
  "config": {
    "step": {
      "user": {
        "title": "Action Control",
        "description": "Set up Action Control. You will define watchdog rules afterwards, from the integration's Configure button."
      }
    },
    "abort": {
      "single_instance_allowed": "Action Control is already configured; only one instance is needed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Action Control",
        "menu_options": {
          "new_rule": "Add a rule",
          "edit_rule_select": "Edit a rule",
          "delete_rule_select": "Delete a rule",
          "global_settings": "Global settings"
        }
      },
      "rule_menu": {
        "title": "Rule – sections",
        "description": "Pick a section to fill in or correct; you come back here each time. Nothing is written until you save.",
        "menu_options": {
          "add_rule": "Targeting",
          "rule_exclude": "What to leave out",
          "add_rule_services": "Services",
          "rule_features": "What it should do",
          "rule_verify": "Verification & retries",
          "rule_escalation": "Recovery action",
          "rule_escalation_check": "Verifying the recovery action",
          "rule_save": "Save the rule"
        },
        "menu_option_descriptions": {
          "add_rule": "{name} — {domains}",
          "rule_exclude": "{excluded} left out",
          "add_rule_services": "{services}",
          "rule_verify": "{retries} retries",
          "rule_escalation_check": "{escalation_check_entity_id}"
        }
      },
      "add_rule": {
        "title": "Rule – what to watch",
        "description": "Choose which domain(s) this rule watches, and which entities it applies to. The following steps let you pick the services to watch within these domains, and optionally what to leave out.",
        "data": {
          "name": "Name",
          "enabled": "Rule enabled",
          "domains": "Domains",
          "entity_id_pattern": "Entity ID pattern (glob, e.g. cover.volet_*)",
          "name_pattern": "Friendly name pattern (glob)",
          "area_ids": "Areas",
          "label_ids": "Labels",
          "device_ids": "Devices",
          "exclusions_enabled": "Leave out some entities or devices"
        }
      },
      "rule_exclude": {
        "title": "Rule – what to leave out",
        "description": "Anything named here is dropped whatever the other filters say. Pick entities to remove them one by one — the usual case being a switch also exposed as a light, watched twice per command. Picking a device removes every entity it exposes, so it is not the way to split such a pair: both belong to the same device. Patterns cover the wider cases. Only the {domains} domain(s) are offered.",
        "data": {
          "entity_id_exclude": "Entities to leave out",
          "device_id_exclude": "Devices to leave out (removes every entity they expose)",
          "entity_id_exclude_patterns": "Entity ID patterns to exclude (glob, e.g. light.salon_multiprise_*)"
        }
      },
      "add_rule_services": {
        "title": "Rule – which services",
        "description": "Services actually available for {domains} are suggested below; leave empty to watch every service in these domains, or type a service name that isn't listed.",
        "data": {
          "services": "Services (empty = any service in the domain)"
        }
      },
      "rule_features": {
        "title": "Rule – what it should do",
        "description": "Pick what this rule uses. The next steps only ask for the settings these choices actually need.",
        "data": {
          "verification_mode": "How to verify the command",
          "escalation_enabled": "Run a recovery action when it keeps failing",
          "log_entity_info": "Log a summary (entity, outcome, response time) for this rule at info level",
          "notify_persistent": "Notify via a persistent notification",
          "notify_service": "Also notify via this notify service"
        }
      },
      "rule_verify": {
        "title": "Rule – verification & retries",
        "description": "Define how the outcome is checked and how retries are handled.",
        "data": {
          "check_delay": "Delay before the first check (seconds)",
          "attributes_to_check": "Attributes to check (in addition to state)",
          "tolerances": "Tolerances, as attr:value, attr2:value2",
          "retries": "Number of retries",
          "retry_delay": "Delay between retries (seconds)",
          "retry_backoff": "Delay growth between retries",
          "change_attribute": "Attribute to watch for a change (e.g. current_position)",
          "change_timeout": "Timeout waiting for the change (seconds)"
        }
      },
      "rule_escalation": {
        "title": "Rule – recovery action",
        "description": "The recovery action (e.g. turning on a switch, or running a script) to run when verification keeps failing.",
        "data": {
          "escalation_action": "Escalation action",
          "escalation_cooldown": "Minimum time between two escalations (seconds)",
          "escalation_replay_delay": "Delay after escalation before replaying the command (seconds)",
          "escalation_check_enabled": "Verify the recovery action worked before replaying the command"
        }
      },
      "rule_escalation_check": {
        "title": "Rule – verifying the recovery action",
        "description": "The recovery action is re-run, using this rule's retry count and delay growth, until this entity reaches the expected state. The original command is replayed either way.",
        "data": {
          "escalation_check_entity_id": "Entity to verify after the recovery action",
          "escalation_check_state": "State it should reach (e.g. on)",
          "escalation_check_delay": "Delay before checking it (seconds)"
        }
      },
      "edit_rule_select": {
        "title": "Edit a rule",
        "data": {
          "rule_id": "Rule"
        }
      },
      "delete_rule_select": {
        "title": "Delete a rule",
        "data": {
          "rule_id": "Rule"
        }
      },
      "delete_rule_confirm": {
        "title": "Confirm deletion",
        "description": "Delete rule \"{rule_name}\"? This cannot be undone.",
        "data": {
          "confirm": "Confirm deletion"
        }
      },
      "global_settings": {
        "title": "Global settings",
        "data": {
          "enabled": "Action Control enabled",
          "default_retries": "Default number of retries for new rules",
          "default_retry_delay": "Default delay between retries for new rules (seconds)"
        }
      }
    },
    "error": {
      "invalid_tolerances": "Tolerances must be written as attribute:value, separated by commas (e.g. brightness:5, rgb_color:5).",
      "domains_required": "Pick at least one domain — a rule with no domain never matches anything.",
      "change_attribute_required": "Movement mode needs an attribute to watch (e.g. current_position).",
      "escalation_check_required": "Verifying the recovery action needs both an entity and the state it should reach."
    },
    "abort": {
      "no_rules": "No rule has been configured yet."
    }
  },
  "entity": {
    "sensor": {
      "rule_status": {
        "state": {
          "idle": "Idle",
          "ok": "OK",
          "retrying": "Retrying",
          "escalated": "Escalated",
          "failed": "Failed"
        }
      }
    }
  },
  "selector": {
    "retry_backoff": {
      "options": {
        "constant": "Constant (same delay every time)",
        "linear": "Linear (delay × attempt number)",
        "exponential": "Exponential (delay doubles each time)"
      }
    },
    "verification_mode": {
      "options": {
        "delay": "Delay — wait, then compare state and attributes",
        "movement": "Movement — wait for an attribute to actually start changing (covers, ...)"
      }
    }
  },
  "services": {
    "run_rule": {
      "name": "Run rule",
      "description": "Test a rule on demand: re-issues the given service on the entity and lets the rule verify it, exactly as if the service had been called normally.",
      "fields": {
        "rule_sensor": {
          "name": "Rule",
          "description": "The rule's status sensor."
        },
        "entity_id": {
          "name": "Entity",
          "description": "The entity to test the rule against."
        },
        "service_data": {
          "name": "Service data",
          "description": "Data for the service call. Include a \"service\" key to override the rule's default service."
        }
      }
    },
    "reset_escalation_cooldown": {
      "name": "Reset escalation cooldown",
      "description": "Clear a rule's escalation cooldown so it can escalate again immediately.",
      "fields": {
        "rule_sensor": {
          "name": "Rule",
          "description": "The rule's status sensor."
        }
      }
    }
  },
  "issues": {
    "stale_target": {
      "title": "Action Control: rule targets a missing area, label, or device",
      "description": "Rule \"{rule}\" targets one or more areas, labels, or devices that no longer exist. It won't match anything until you update or remove that target in the rule's targeting step."
    }
  }
}
