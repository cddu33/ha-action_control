See [AGENTS.md](AGENTS.md) for how to work on this repository — module map,
bilingual and translation rules, codebase-specific traps, test commands, and
the branch/PR/release conventions.

Kept as a pointer rather than a copy: duplicated guidance drifts, which is
exactly the failure mode `AGENTS.md` warns about for `strings.json`.
